import { FC } from "react";

const LoadingSpinner: FC = () => {
  return (
    <div className="h-full pos-center">
      <div className="loader"></div>
    </div>
  );
};

export default LoadingSpinner;
